(function(get_socket){ return get_socket() })
(function(){
    return function export_socket(file,...options){
        
        console.log('socket',file,options)
        
        return new Promise((success,error)=>{
        
        })
    }
})